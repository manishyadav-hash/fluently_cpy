# Fluently Admin

React + Tailwind CSS project setup for the Fluently admin panel.

## Run Locally

```bash
npm install
npm run dev
```

The local admin panel runs at:

```text
http://localhost:5174
```
